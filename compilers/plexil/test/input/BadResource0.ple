Integer Command c1;

// missing Priority
Root: {
  Integer returnValue = -1;
  EndCondition returnValue == 10;
  Resource Name = "arm";
  Resource Name = "leg", UpperBound = 20, UpperBound = 30; // duplicate UpperBound
  returnValue = c1();
}
