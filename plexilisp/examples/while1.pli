(plexil-plan
 (list-node "while-test"
  (variables (integer "count" 0))
  (list
   (while (< (intvar "count") 2)
     (empty-node "foo")
     (command "bar")
     (assignment (intvar "count") (+ (intvar "count") 1))))))



 
