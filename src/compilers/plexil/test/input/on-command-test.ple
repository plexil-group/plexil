Command HaltAndCatchFire;
Command ExecuteProgrammerImmediate;

on-command-test:
Concurrence
{
	String hcfName = "HCF";

	OnCommand hcfName
      HaltAndCatchFire();

	OnCommand "EPI"
      ExecuteProgrammerImmediate();

  OnCommand "Sum" (Integer number1, Integer number2) 
    Increment: { SendReturnValues(number + number2); }

}
