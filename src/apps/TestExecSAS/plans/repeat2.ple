Wrapper0:
{
  EndCondition: LookupOnChange(x) != 0;
   
  NodeList:
  Wrapper1:
   {
     NodeList:
     Wrapper1:
      {
        NodeList:
        FooCall:
         {
           RepeatCondition: true;
           Command: foo();
         }
      }
   }
}
