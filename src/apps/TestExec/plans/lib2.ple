LibraryNode LibTest1(In Boolean lb, In Integer li, In Real lr, In String ls); //, In Time lt); // In BLOB lbl,
LibraryNode LibTest2(In Real y, In Real z);

LibTest2:
{
   In Real y;
   In Real z;

   
  NodeList:

      // call command bar()
   
  BarCall2: {Command: bar(y, z);}

      // call LibTest1 (from LibTest2) libraries calling libraries

  Call2LibTest1:
   {
     LibraryCall: LibTest1(lb = false, li = 123, lr = y, ls = "what!");
   }
}
