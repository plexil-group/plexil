LibraryNode Ack (In Integer m, In Integer n, InOut Integer result);

LibraryCallTest:
{
  Integer result;

  Sequence:

    Ack1: { LibraryCall: Ack (m=0, n=0, result=result); }
    Print1: { Command: pprint ("Ack(0,0) =", result); }
}
