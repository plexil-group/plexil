//The controlling plan
//Subscription commands are sent using SendMessage("<command name>")
MasterPlan:
{
	String name = "n.a";
	String ret[2];
	Sequence
		Action:
		{
			EndCondition: isKnown(ret[0]) && isKnown(ret[1]);
			Command: ret = GetName();
		}
		Set:
		{
			Assignment: name = ret[0] + " " + ret[1];
		}
		Check:
		{
			InvariantCondition: isKnown(name);
		}
			
}
