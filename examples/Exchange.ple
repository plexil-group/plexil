// Example that illustrates synchronous execution of NodeList
// chidren.

Exchange:
{
       Integer x = 1;
       Integer y = 2;

       Invariant: x + y == 3;

       Post: !(x == y);

       NodeList:  
               X:
               {
                       Assignment: x = y;
               }

               Y:
               {
                       Assignment: y = x;
               }  
}
