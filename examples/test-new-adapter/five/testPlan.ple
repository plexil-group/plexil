Integer Lookup lookups1;
Integer Lookup lookups2;
Integer Lookup lookups3;
Integer Lookup lookups4;
Integer Lookup lookups5;

Command pprint(...);
Command resetLookups1();
Command resetLookups2();
Command resetLookups3();
Command resetLookups4();
Command resetLookups5();

testPlan:
{
  Integer lookups;
  for(Integer i = 0; i < 500000; i + 1)
  {
    lookups = Lookup(lookups1);
    resetLookups1();
    lookups = Lookup(lookups2);
    resetLookups2();
    lookups = Lookup(lookups3);
    resetLookups3();
    lookups = Lookup(lookups4);
    resetLookups4();
    lookups = Lookup(lookups5);
    resetLookups5();
  }
}
