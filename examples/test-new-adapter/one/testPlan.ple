Integer Lookup lookups;

Command pprint(...);
Command resetLookups();

testPlan:
{
  Integer lookups;
  for(Integer i = 0; i < 1000000; i + 1)
  {
    lookups = Lookup(lookups);
    resetLookups();
  }
}
