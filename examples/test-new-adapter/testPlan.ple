Integer Lookup lookups;

Command pprint(...);
Command resetLookups();

testPlan:
{
  for(Integer i = 0; i < 10; i + 1)
  {
    for(Integer j = 0; j < 10; j + 1)
    {
      pprint(Lookup(lookups));
    }
    resetLookups();
  }
}
